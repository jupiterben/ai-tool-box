# GitHub Actions：PR / main 分支持续集成（验证前端 + Electron 主进程可编译）

name: CI

on:
  push:
    branches:
      - main
      - master
  pull_request:
    branches:
      - main
      - master

jobs:
  check:
    name: Compile Check
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 9

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm

      - name: Configure public npm registry
        run: |
          cat > .npmrc <<'EOF'
          enable-pre-post-scripts=true
          ignore-scripts=false
          registry=https://registry.npmjs.org/
          EOF

      - name: Install dependencies
        run: pnpm install

      - name: Build frontend & electron main
        run: |
          pnpm run generate:icon
          pnpm run build
          pnpm run electron:compile
